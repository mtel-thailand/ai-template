name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality-gate:
    name: Quality Gate
    uses: ./.github/workflows/quality-gate.yml
    with:
      node-version: '22'
      audit-level: high
      bundle-budget-kb: 100
      coverage-threshold: 80
